dermethic-gestao/
├── index.html            ← Dashboard principal (entry point)
├── clientes.html         ← CRM + processo de onboarding
├── fornecedores.html     ← Fornecedores + qualificação + SOP compras
├── terceirizados.html    ← Parceiros + contratos + SLA
├── formulacao.html       ← P&D + fichas técnicas + Anvisa
├── tecnico.html          ← Ingredientes + claims + protocolos
├── financeiro.html       ← Custos + NFs + DRE + BOM
├── marketing.html        ← Personas + lançamento + campanhas
├── treinamento.html      ← Vendas por canal (farmácias, salões...)
├── comunicacao.html      ← IA Chat (Anthropic API, 8 modos)
├── configuracoes.html    ← API, backup, deploy
├── css/
│   └── style.css         ← Design system compartilhado
└── js/
    └── db.js             ← Banco de dados + seed data + utilitários
